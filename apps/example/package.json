{
  "name": "example",
  "version": "0.1.0",
  "private": true,
  "description": "@trade/ui example — component reference (watchlist + order ticket).",
  "scripts": {
    "build": "node ../../scripts/build-example.mjs"
  }
}
